FUNÇÕES USADAS DURANTE O MEU PROGRAMA:

al_draw_bitmap(imagem, x, y, flags);

void al_draw_text(
    const ALLEGRO_FONT *font,  // Fonte usada
    ALLEGRO_COLOR color,       // Cor do texto
    float x,                   // Posição X na tela
    float y,                   // Posição Y na tela
    int flags,                 // Alinhamento do texto
    const char *text           // Texto que será desenhado
);

ALLEGRO_KEY_UP — seta para cima

ALLEGRO_KEY_DOWN — seta para baixo

ALLEGRO_KEY_LEFT — seta para esquerda

ALLEGRO_KEY_RIGHT — seta para direita

/ Exibir o estado atual
if (estado == START) {
    al_draw_text(font, al_map_rgb(0, 255, 0), 540, 500, ALLEGRO_ALIGN_CENTER, "JOGO INICIADO...");
} else if (estado == MENU) {
    al_draw_text(font, al_map_rgb(0, 255, 255), 540, 500, ALLEGRO_ALIGN_CENTER, "TELA DE MENU...");
}

